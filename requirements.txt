# Dependencias mínimas para el proyecto Malta_Store
# Recomendación: pincha versiones tras validar en tu entorno (ej. pip freeze > requirements.txt)
fastapi
uvicorn[standard]
flask
requests
httpx
jinja2
